class Solution {
public:
    int divide(int dividend, int divisor) {
        
        long ans = (long)((long)dividend / divisor);
    
    if(ans < INT_MIN or ans > INT_MAX)
        return INT_MAX;
    return (int)(ans);
       
        
        
        
    }
};
